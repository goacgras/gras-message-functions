const { db, admin } = require("../util/admin");

//TODO add room when user create contact by send params and set access to true
//TODO add userContact to destinateUser when user send message to destinateUser
exports.addRoom = (req, res) => {
  //   db.collection('rooms').doc(req.user.uid).collection('userRooms').add({
  //   })
};
